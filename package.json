{
  "name": "explicit-web",
  "version": "1.0.0",
  "description": "ComplianceHQ — compliance docs assistant (web)",
  "private": true,
  "scripts": {
    "start": "node web/server.js",
    "dev": "node --watch web/server.js",
    "test:compliance": "npm run test:compliance --prefix web",
    "test:compliance:ui": "npm run test:compliance:ui --prefix web",
    "test:compliance:headed": "npm run test:compliance:headed --prefix web",
    "test:compliance:report": "npm run test:compliance:report --prefix web",
    "fixtures:generate": "npm run fixtures:generate --prefix web"
  }
}
